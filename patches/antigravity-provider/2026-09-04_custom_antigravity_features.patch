diff --git a/src/antigravity_provider/antigravity_client.py b/src/antigravity_provider/antigravity_client.py
index 5f3732a..d198c0a 100644
--- a/src/antigravity_provider/antigravity_client.py
+++ b/src/antigravity_provider/antigravity_client.py
@@ -13,8 +13,8 @@ ANTIGRAVITY_ENDPOINTS = [
     "https://daily-cloudcode-pa.sandbox.googleapis.com",
 ]
 MODEL_CATALOG_ENDPOINTS = [
-    "https://cloudcode-pa.googleapis.com",
     "https://daily-cloudcode-pa.googleapis.com",
+    "https://cloudcode-pa.googleapis.com",
 ]
 STREAM_PATH = "/v1internal:streamGenerateContent?alt=sse"
 MODEL_CATALOG_PATH = "/v1internal:fetchAvailableModels"
diff --git a/src/antigravity_provider/cloudcode.py b/src/antigravity_provider/cloudcode.py
index 2d432f1..52a3877 100644
--- a/src/antigravity_provider/cloudcode.py
+++ b/src/antigravity_provider/cloudcode.py
@@ -22,7 +22,7 @@ PROJECT_ONBOARD_INTERVAL_SECONDS = 2
 
 
 def antigravity_user_agent() -> str:
-    version = os.getenv("PI_AI_ANTIGRAVITY_VERSION") or "2.1.4"
+    version = os.getenv("PI_AI_ANTIGRAVITY_VERSION") or "2.11.0"
     system = platform.system().lower()
     os_name = "windows" if system.startswith("win") else ("darwin" if system == "darwin" else system or "linux")
     machine = platform.machine().lower()
diff --git a/src/antigravity_provider/hermes_plugin.py b/src/antigravity_provider/hermes_plugin.py
index 237fb60..f0acbfb 100644
--- a/src/antigravity_provider/hermes_plugin.py
+++ b/src/antigravity_provider/hermes_plugin.py
@@ -203,51 +203,126 @@ def _handle_cli(args: argparse.Namespace) -> None:
 
 
 
+def _quota_display_name(model_id: str) -> str:
+    normalized = re.sub(r"(?<=\d)-(?=\d(?:-|$))", ".", model_id)
+    if normalized.startswith("gpt-oss-"):
+        return "GPT-OSS " + normalized.removeprefix("gpt-oss-").upper()
+    return " ".join(part.capitalize() for part in normalized.split("-"))
+
+
 def _format_quota_markdown(raw_models: dict[str, Any]) -> str:
-    from datetime import datetime, timezone
-    myt = timezone(datetime.now().astimezone().utcoffset() or timezone.utc.utcoffset(None))
-
-    target_keys = [
-        ("Gemini 3.7 Flash", ("gemini-3.7-flash-tiered", "gemini-3.7-flash")),
-        ("Gemini 3.6 Flash", ("gemini-3.6-flash-tiered", "gemini-3.6-flash-high", "gemini-3.6-flash")),
-        ("Gemini 3.1 Pro", ("gemini-3.1-pro-high", "gemini-pro-agent", "gemini-3.1-pro")),
-        ("Gemini 3.5 Flash", ("gemini-3-flash-agent", "gemini-3.5-flash-low", "gemini-3.5-flash")),
-        ("Claude Sonnet 4.6", ("claude-sonnet-4-6",)),
-        ("Claude Opus 4.6", ("claude-opus-4-6-thinking", "claude-opus-4-6")),
-        ("GPT-OSS 120B", ("openai/gpt-oss-120b-maas", "gpt-oss-120b-medium", "gpt-oss-120b")),
-    ]
-    lines = [
-        "📈 **Antigravity Live Account Quota**",
-        "Provider: `antigravity` (Google Cloud Code Assist)",
-        "",
-    ]
-    for label, wire_keys in target_keys:
-        info: dict = {}
-        for k in wire_keys:
-            if k in raw_models and isinstance(raw_models[k], dict):
-                info = raw_models[k]
-                break
-        q = info.get("quotaInfo") or {}
+    from datetime import datetime
+    from zoneinfo import ZoneInfo
+
+    from .models import VERIFIED_PICKER_SUPPLEMENTS, logical_model_id
+
+    myt = ZoneInfo("Asia/Kuala_Lumpur")
+
+    def quota_parts(info: dict[str, Any]) -> tuple[str, str, str]:
+        quota_info = info.get("quotaInfo")
+        q = quota_info if isinstance(quota_info, dict) else {}
         rem = q.get("remainingFraction")
         reset_raw = q.get("resetTime")
         if isinstance(rem, (int, float)):
             rem_str = f"{rem * 100:.1f}%"
-            used_str = f"{(1.0 - rem) * 100:.1f}% used"
+            used_part = f" ({(1.0 - rem) * 100:.1f}% used)"
         else:
             rem_str = "N/A"
-            used_str = ""
-
+            used_part = ""
         if reset_raw:
             try:
-                dt = datetime.fromisoformat(reset_raw.replace("Z", "+00:00")).astimezone(myt)
+                dt = datetime.fromisoformat(str(reset_raw).replace("Z", "+00:00")).astimezone(myt)
                 reset_str = dt.strftime("%d/%m %I:%M %p MYT")
-            except Exception:
+            except (TypeError, ValueError):
                 reset_str = str(reset_raw)
         else:
             reset_str = "N/A"
+        return rem_str, used_part, reset_str
+
+    rows: dict[str, list[tuple[str, dict[str, Any]]]] = {}
+    for wire_id in sorted(raw_models):
+        if not isinstance(wire_id, str):
+            continue
+        logical_id = logical_model_id(wire_id)
+        info = raw_models.get(wire_id)
+        if not logical_id or not isinstance(info, dict):
+            continue
+        rows.setdefault(logical_id, []).append((wire_id, info))
+
+    for model_id in VERIFIED_PICKER_SUPPLEMENTS:
+        if model_id not in rows:
+            rows[model_id] = []
+
+    def gemini_sort_key(logical_id: str) -> tuple[list[float], int]:
+        nums = [float(x) for x in re.findall(r"\d+(?:\.\d+)?", logical_id.replace("-", "."))]
+        tier = 0
+        if "flash" in logical_id and "lite" not in logical_id:
+            tier = 3
+        elif "pro" in logical_id:
+            tier = 2
+        elif "lite" in logical_id:
+            tier = 1
+        return (nums, tier)
+
+    def other_sort_key(logical_id: str) -> tuple[int, list[float], int]:
+        family_weight = 0
+        if logical_id.startswith("claude"):
+            family_weight = 2
+        elif logical_id.startswith("gpt"):
+            family_weight = 1
+        nums = [float(x) for x in re.findall(r"\d+(?:\.\d+)?", logical_id.replace("-", "."))]
+        sub_weight = 0
+        if "sonnet" in logical_id:
+            sub_weight = 2
+        elif "opus" in logical_id:
+            sub_weight = 1
+        return (family_weight, nums, sub_weight)
+
+    gemini_ids = sorted([m for m in rows if m.startswith("gemini-")], key=gemini_sort_key, reverse=True)
+    other_ids = sorted([m for m in rows if not m.startswith("gemini-")], key=other_sort_key, reverse=True)
 
-        used_part = f" ({used_str})" if used_str else ""
-        lines.append(f"• **{label}**: `{rem_str}` baki{used_part} • Reset: `{reset_str}`")
+    lines = [
+        "📈 **Antigravity Live Account Quota**",
+        "Provider: `antigravity` (Google Cloud Code Assist)",
+        "",
+    ]
+
+    def render_section(title: str, ids: list[str]) -> None:
+        if not ids:
+            return
+        lines.append(f"🔹 **{title}**")
+        lines.append("")
+        for idx, logical_id in enumerate(ids, 1):
+            label = _quota_display_name(logical_id)
+            variants = rows[logical_id]
+            if not variants:
+                lines.append(f"{idx}. **{label}**:")
+                lines.append("   • quota unavailable — not returned by bound account live catalog")
+                lines.append("")
+                continue
+
+            parts = [(wire_id, *quota_parts(info)) for wire_id, info in variants]
+            signatures = {(rem_str, used_part, reset_str) for _wire_id, rem_str, used_part, reset_str in parts}
+            if len(signatures) == 1:
+                _wire_id, rem_str, used_part, reset_str = parts[0]
+                lines.append(f"{idx}. **{label}**:")
+                lines.append(f"   • {rem_str} baki{used_part}")
+                lines.append(f"   • Reset: {reset_str}")
+                lines.append("")
+                continue
+
+            lines.append(f"{idx}. **{label}** (quota varies by route):")
+            for wire_id, rem_str, used_part, reset_str in parts:
+                lines.append(f"   • `{wire_id}`: {rem_str} baki{used_part} • Reset: {reset_str}")
+            lines.append("")
+
+    if gemini_ids:
+        render_section("Gemini Models", gemini_ids)
+    if other_ids:
+        render_section("Other Models", other_ids)
+
+    while lines and lines[-1] == "":
+        lines.pop()
 
     return "\n".join(lines)
 
diff --git a/src/antigravity_provider/hermes_provider.py b/src/antigravity_provider/hermes_provider.py
index 1e71df9..d188df4 100644
--- a/src/antigravity_provider/hermes_provider.py
+++ b/src/antigravity_provider/hermes_provider.py
@@ -2,7 +2,12 @@ from __future__ import annotations
 
 from typing import Any
 
-from .models import DEFAULT_MODEL, KNOWN_MODELS, clamp_reasoning_effort
+from .models import (
+    DEFAULT_MODEL,
+    KNOWN_MODELS,
+    VERIFIED_PICKER_SUPPLEMENTS,
+    clamp_reasoning_effort,
+)
 
 PROVIDER_NAME = "antigravity"
 PLACEHOLDER_API_KEY_ENV = "ANTIGRAVITY_HERMES_API_KEY"
@@ -10,6 +15,31 @@ PLACEHOLDER_API_KEY = "hermes-plugin"
 DUMMY_BASE_URL = "http://127.0.0.1:8765/v1"
 
 
+def fetch_picker_model_ids() -> list[str]:
+    """Return the live Antigravity catalog as stable logical picker IDs."""
+    try:
+        from .catalog import logical_model_ids_from_catalog
+        from .runtime import fetch_available_models
+
+        live = logical_model_ids_from_catalog(fetch_available_models())
+    except Exception:
+        return []
+    if not live:
+        return []
+
+    result: list[str] = []
+    seen: set[str] = set()
+    for model_id in [*live, *VERIFIED_PICKER_SUPPLEMENTS]:
+        if not isinstance(model_id, str) or not model_id.strip():
+            continue
+        model_id = model_id.strip()
+        key = model_id.casefold()
+        if key not in seen:
+            result.append(model_id)
+            seen.add(key)
+    return result
+
+
 def _reasoning_effort(reasoning_config: dict | None, model: str | None = None) -> str | None:
     if not isinstance(reasoning_config, dict):
         return None
@@ -48,16 +78,7 @@ def register_provider_profile() -> bool:
             return KNOWN_MODELS.get(model or "") or KNOWN_MODELS.get(DEFAULT_MODEL)
 
         def fetch_models(self, **kwargs: Any) -> list[str] | None:
-            try:
-                from .catalog import logical_model_ids_from_catalog
-                from .runtime import fetch_available_models
-
-                live = logical_model_ids_from_catalog(fetch_available_models())
-                if live:
-                    return live
-            except Exception:
-                pass
-            return list(KNOWN_MODELS)
+            return fetch_picker_model_ids() or list(KNOWN_MODELS)
 
     register_provider(
         AntigravityProfile(
diff --git a/src/antigravity_provider/models.py b/src/antigravity_provider/models.py
index 42a30d5..f1c03f4 100644
--- a/src/antigravity_provider/models.py
+++ b/src/antigravity_provider/models.py
@@ -6,24 +6,27 @@ ANTIGRAVITY_PREFIX = "google-antigravity/"
 DEFAULT_MODEL = "gemini-3.7-flash"
 
 KNOWN_MODELS: dict[str, int] = {
+    "gemini-3.8-flash": 65536,
     "gemini-3.7-flash": 65536,
     "gemini-3.6-flash": 65536,
     "gemini-3.1-pro": 65535,
-    "gemini-3.5-flash": 65536,
     "claude-sonnet-4-6": 64000,
     "claude-opus-4-6": 64000,
     "gpt-oss-120b": 65536,
-    # Backward compatibility with fully-qualified prefix
-    "google-antigravity/gemini-3.7-flash": 65536,
-    "google-antigravity/gemini-3.6-flash": 65536,
-    "google-antigravity/gemini-3.1-pro": 65535,
-    "google-antigravity/gemini-3.5-flash": 65536,
-    "google-antigravity/claude-sonnet-4-6": 64000,
-    "google-antigravity/claude-opus-4-6": 64000,
-    "google-antigravity/gpt-oss-120b": 65536,
+    "gemini-3-flash": 65536,
+    "gemini-2.5-flash": 65536,
 }
 
+# Cloud Code's live entitlement catalog currently omits this documented,
+# directly callable Antigravity model. Keep the exception explicit and narrow.
+VERIFIED_PICKER_SUPPLEMENTS: tuple[str, ...] = (
+    "gemini-3.8-flash",
+)
+
 WIRE_PROFILES: dict[str, dict[str, object]] = {
+    "gemini-3.8-flash-low": {"maxOutputTokens": 65536},
+    "gemini-3.8-flash-medium": {"maxOutputTokens": 65536},
+    "gemini-3.8-flash-high": {"maxOutputTokens": 65536},
     "gemini-3.7-flash-tiered": {"maxOutputTokens": 65536},
     "gemini-3.6-flash-tiered": {"maxOutputTokens": 65536},
     "gemini-3.5-flash-extra-low": {"modelEnum": "MODEL_PLACEHOLDER_M187", "maxOutputTokens": 65536},
@@ -125,6 +128,8 @@ def _effort(effort: str | None) -> str:
 def clamp_reasoning_effort(model: str, reasoning_effort: str | None = None) -> str:
     logical = strip_provider_prefix(normalize_model_id(model))
     effort = _effort(reasoning_effort)
+    if logical == "gemini-3.8-flash" and effort == "off":
+        return "low"
     if effort == "off":
         return "off"
     if logical == "gemini-3.1-pro":
@@ -135,6 +140,8 @@ def clamp_reasoning_effort(model: str, reasoning_effort: str | None = None) -> s
         if effort == "medium":
             return "medium"
         return "low"
+    if logical == "gemini-3.8-flash":
+        return effort if effort in {"low", "medium", "high"} else "low"
     if logical in {"gpt-oss-120b", "openai/gpt-oss-120b-maas"}:
         return "medium"
     return effort
@@ -153,6 +160,8 @@ def resolve_wire_model_id(model: str, reasoning_effort: str | None = None) -> st
         if effort == "medium":
             return "gemini-3.5-flash-low"
         return "gemini-3.5-flash-extra-low"
+    if logical == "gemini-3.8-flash":
+        return f"gemini-3.8-flash-{effort}"
     if logical == "claude-opus-4-6":
         return "claude-opus-4-6-thinking"
     if logical == "claude-sonnet-4-6":
diff --git a/src/antigravity_provider/transform.py b/src/antigravity_provider/transform.py
index 2c468cb..bf71d0c 100644
--- a/src/antigravity_provider/transform.py
+++ b/src/antigravity_provider/transform.py
@@ -77,7 +77,7 @@ def _parse_args(raw: Any) -> dict[str, Any]:
     return {}
 
 
-def _schema(schema: Any) -> dict[str, Any]:
+def _schema(schema: Any, *, claude_compat: bool = False, tool_name: str = "") -> dict[str, Any]:
     if not isinstance(schema, dict):
         return {"type": "object", "properties": {}}
     banned = {"$schema", "$defs", "definitions", "additionalProperties", "patternProperties", "unevaluatedProperties"}
@@ -91,10 +91,32 @@ def _schema(schema: Any) -> dict[str, Any]:
     if "type" not in out:
         out["type"] = "object"
     out.setdefault("properties", {})
+    # Cloud Code Assist's Claude route rejects the union used by Hermes'
+    # terminal `notify` option (boolean or line-match list), even though it
+    # is valid JSON Schema.  Keep the full schema for Gemini; advertise the
+    # supported boolean form only on Claude so the whole tool list validates.
+    if claude_compat and tool_name == "terminal":
+        properties = out.get("properties")
+        notify = properties.get("notify") if isinstance(properties, dict) else None
+        variants = notify.get("anyOf") if isinstance(notify, dict) else None
+        if isinstance(notify, dict) and isinstance(variants, list):
+            boolean_variant = next(
+                (item for item in variants if isinstance(item, dict) and item.get("type") == "boolean"),
+                None,
+            )
+            array_variant = next(
+                (item for item in variants if isinstance(item, dict) and item.get("type") == "array"),
+                None,
+            )
+            if boolean_variant is not None and array_variant is not None:
+                replacement = dict(boolean_variant)
+                if notify.get("description"):
+                    replacement["description"] = notify["description"]
+                properties["notify"] = replacement
     return out
 
 
-def _tools(tools: list[dict[str, Any]]) -> list[dict[str, Any]] | None:
+def _tools(tools: list[dict[str, Any]], *, claude_compat: bool = False) -> list[dict[str, Any]] | None:
     declarations = []
     for tool in tools or []:
         if not isinstance(tool, dict) or tool.get("type") != "function":
@@ -106,7 +128,11 @@ def _tools(tools: list[dict[str, Any]]) -> list[dict[str, Any]] | None:
             {
                 "name": fn["name"],
                 "description": fn.get("description") or "",
-                "parameters": _schema(fn.get("parameters")),
+                "parameters": _schema(
+                    fn.get("parameters"),
+                    claude_compat=claude_compat,
+                    tool_name=str(fn["name"]),
+                ),
             }
         )
     return [{"functionDeclarations": declarations}] if declarations else None
@@ -190,17 +216,36 @@ def build_generate_content_request(
                 name = fn.get("name") if isinstance(fn, dict) else None
                 if not name:
                     continue
-                if tool_call.get("id"):
-                    call_names[str(tool_call["id"])] = name
-                part = {"functionCall": {"name": name, "args": _parse_args(fn.get("arguments") if isinstance(fn, dict) else None)}}
+                call_id = tool_call.get("id") or tool_call.get("call_id")
+                if call_id:
+                    call_names[str(call_id)] = name
+                function_call = {
+                    "name": name,
+                    "args": _parse_args(fn.get("arguments") if isinstance(fn, dict) else None),
+                }
+                # Cloud Code's Claude adapter translates this part to an
+                # Anthropic tool_use block, which requires a stable ID. Hermes
+                # already persists one on every normal tool call; forward it.
+                if wire_model.startswith("claude-") and call_id:
+                    function_call["id"] = str(call_id)
+                part = {"functionCall": function_call}
                 if wire_model.startswith("gemini-3") or wire_model.startswith("gemini-pro"):
                     part["thoughtSignature"] = SKIP_THOUGHT_SIGNATURE
                 parts.append(part)
             if parts:
                 contents.append({"role": "model", "parts": parts})
         elif role == "tool":
-            name = msg.get("name") or call_names.get(str(msg.get("tool_call_id") or "")) or "tool"
-            part = {"functionResponse": {"name": name, "response": {"output": _content_text(msg.get("content"))}}}
+            tool_call_id = str(msg.get("tool_call_id") or "")
+            name = msg.get("name") or call_names.get(tool_call_id) or "tool"
+            function_response = {
+                "name": name,
+                "response": {"output": _content_text(msg.get("content"))},
+            }
+            # Claude's Cloud Code adapter maps this to an Anthropic
+            # tool_result, which must point back to the assistant tool_use ID.
+            if wire_model.startswith("claude-") and tool_call_id:
+                function_response["id"] = tool_call_id
+            part = {"functionResponse": function_response}
             if contents and contents[-1].get("role") == "user" and any("functionResponse" in p for p in contents[-1].get("parts", [])):
                 contents[-1]["parts"].append(part)
             else:
@@ -211,9 +256,21 @@ def build_generate_content_request(
 
     profile = WIRE_PROFILES.get(wire_model, {})
     cap = int(profile.get("maxOutputTokens") or 65535)
+
+    thinking_budget = _thinking_budget(logical, effort)
+    output_tokens = min(max_tokens, cap) if isinstance(max_tokens, int) and max_tokens > 0 else cap
+
+    # Claude on Vertex AI extended thinking invariant: maxOutputTokens must be > thinkingBudget
+    if "claude" in wire_model.lower() or "claude" in logical.lower():
+        if effort != "off":
+            if output_tokens <= thinking_budget:
+                # Guarantee headroom for output text
+                output_tokens = max(output_tokens, thinking_budget + 2048)
+                output_tokens = min(output_tokens, cap)
+
     generation_config: dict[str, Any] = {
-        "maxOutputTokens": min(max_tokens, cap) if isinstance(max_tokens, int) and max_tokens > 0 else cap,
-        "thinkingConfig": {"includeThoughts": effort != "off", "thinkingBudget": _thinking_budget(logical, effort)},
+        "maxOutputTokens": output_tokens,
+        "thinkingConfig": {"includeThoughts": effort != "off", "thinkingBudget": thinking_budget},
     }
     if temperature is not None:
         generation_config["temperature"] = temperature
@@ -228,7 +285,7 @@ def build_generate_content_request(
     }
     if system_parts:
         request["systemInstruction"] = {"role": "system", "parts": system_parts}
-    converted_tools = _tools(tools or [])
+    converted_tools = _tools(tools or [], claude_compat=wire_model.startswith("claude-"))
     if converted_tools:
         request["tools"] = converted_tools
     config = _tool_config(tools or [], tool_choice, wire_model)
diff --git a/tests/test_cloudcode.py b/tests/test_cloudcode.py
index de1879e..02e83cf 100644
--- a/tests/test_cloudcode.py
+++ b/tests/test_cloudcode.py
@@ -1,6 +1,11 @@
 import unittest
 
-from antigravity_provider.cloudcode import load_or_onboard_project, read_default_tier, read_project_id
+from antigravity_provider.cloudcode import (
+    antigravity_user_agent,
+    load_or_onboard_project,
+    read_default_tier,
+    read_project_id,
+)
 
 
 class CloudCodeTests(unittest.TestCase):
@@ -13,6 +18,15 @@ class CloudCodeTests(unittest.TestCase):
         self.assertEqual(read_default_tier([{"id": "free"}, {"id": "pro", "isDefault": True}]), "pro")
         self.assertEqual(read_default_tier([]), "legacy-tier")
 
+    def test_user_agent_defaults_to_current_supported_desktop_release(self):
+        import os
+        previous = os.environ.pop("PI_AI_ANTIGRAVITY_VERSION", None)
+        try:
+            self.assertIn("antigravity/hub/2.11.0", antigravity_user_agent())
+        finally:
+            if previous is not None:
+                os.environ["PI_AI_ANTIGRAVITY_VERSION"] = previous
+
     def test_onboards_when_no_project(self):
         calls = []
         def fake_post(url, body, headers):
diff --git a/tests/test_hermes_plugin.py b/tests/test_hermes_plugin.py
index 35222c5..df4c854 100644
--- a/tests/test_hermes_plugin.py
+++ b/tests/test_hermes_plugin.py
@@ -55,10 +55,98 @@ class HermesPluginTests(unittest.TestCase):
             }
         }
         res = plugin._format_quota_markdown(raw)
-        self.assertIn("Gemini 3.7 Flash", res)
-        self.assertIn("`85.0%` baki", res)
-        self.assertIn("(15.0% used)", res)
-        self.assertIn("MYT", res)
+        self.assertIn("🔹 **Gemini Models**", res)
+        self.assertIn("**Gemini 3.7 Flash**:", res)
+        self.assertIn("• 85.0% baki (15.0% used)", res)
+        self.assertIn("• Reset: 31/08 01:22 AM MYT", res)
+        self.assertNotIn("`85.0%`", res)
+
+    def test_format_quota_markdown_renders_live_catalog_models(self):
+        raw = {
+            "gemini-3.8-flash-low": {
+                "quotaInfo": {
+                    "remainingFraction": 0.42,
+                    "resetTime": "2026-09-04T21:42:36Z",
+                }
+            },
+            "gemini-9.9-flash-low": {
+                "quotaInfo": {
+                    "remainingFraction": 0.80,
+                    "resetTime": "2026-09-04T21:42:36Z",
+                }
+            },
+            "claude-sonnet-4-6": {
+                "quotaInfo": {
+                    "remainingFraction": 1.0,
+                    "resetTime": "2026-09-04T21:42:36Z",
+                }
+            },
+        }
+        res = plugin._format_quota_markdown(raw)
+        self.assertIn("🔹 **Gemini Models**", res)
+        self.assertIn("🔹 **Other Models**", res)
+        self.assertIn("1. **Gemini 9.9 Flash**:", res)
+        self.assertIn("2. **Gemini 3.8 Flash**:", res)
+        self.assertIn("1. **Claude Sonnet 4.6**:", res)
+        self.assertIn("• 42.0% baki (58.0% used)", res)
+        self.assertIn("• 80.0% baki (20.0% used)", res)
+        self.assertIn("• 100.0% baki (0.0% used)", res)
+
+    def test_format_quota_markdown_marks_unreported_picker_supplement(self):
+        raw = {
+            "gemini-3.7-flash-tiered": {
+                "quotaInfo": {
+                    "remainingFraction": 0.85,
+                    "resetTime": "2026-09-04T21:42:36Z",
+                }
+            }
+        }
+        res = plugin._format_quota_markdown(raw)
+        self.assertIn(
+            "• quota unavailable — not returned by bound account live catalog",
+            res,
+        )
+        self.assertNotIn("`85.0%`", res)
+
+    def test_format_quota_markdown_preserves_variant_quota_truth(self):
+        equal_routes = {
+            "gemini-3.7-flash-high": {
+                "quotaInfo": {"remainingFraction": 0.80, "resetTime": "2026-09-04T21:42:36Z"}
+            },
+            "gemini-3.7-flash-low": {
+                "quotaInfo": {"remainingFraction": 0.80, "resetTime": "2026-09-04T21:42:36Z"}
+            },
+        }
+        equal_output = plugin._format_quota_markdown(equal_routes)
+        self.assertEqual(equal_output.count("**Gemini 3.7 Flash**"), 1)
+        self.assertNotIn("quota varies by route", equal_output)
+
+        conflicting_routes = {
+            "gemini-3.7-flash-high": {
+                "quotaInfo": {"remainingFraction": 0.80, "resetTime": "2026-09-04T21:42:36Z"}
+            },
+            "gemini-3.7-flash-low": {
+                "quotaInfo": {"remainingFraction": 0.20, "resetTime": "2026-09-04T22:42:36Z"}
+            },
+        }
+        conflicting_output = plugin._format_quota_markdown(conflicting_routes)
+        self.assertIn("quota varies by route", conflicting_output)
+        self.assertIn("gemini-3.7-flash-high", conflicting_output)
+        self.assertIn("80.0% baki", conflicting_output)
+        self.assertIn("gemini-3.7-flash-low", conflicting_output)
+        self.assertIn("20.0% baki", conflicting_output)
+
+    def test_format_quota_markdown_tolerates_noncanonical_quota_values(self):
+        raw = {
+            "gemini-3.7-flash-high": {
+                "quotaInfo": {
+                    "remainingFraction": {"unexpected": 0.80},
+                    "resetTime": ["unexpected"],
+                }
+            }
+        }
+        res = plugin._format_quota_markdown(raw)
+        self.assertIn("• N/A baki", res)
 
     def test_middleware_passthrough_for_other_provider(self):
         seen = []
diff --git a/tests/test_models.py b/tests/test_models.py
index 89cc98f..a4cb8d4 100644
--- a/tests/test_models.py
+++ b/tests/test_models.py
@@ -2,6 +2,7 @@ import unittest
 
 from antigravity_provider.models import (
     KNOWN_MODELS,
+    WIRE_PROFILES,
     clamp_reasoning_effort,
     logical_model_id,
     normalize_model_id,
@@ -33,6 +34,33 @@ class ModelCatalogTests(unittest.TestCase):
             "gemini-3.7-flash-tiered",
         )
 
+    def test_routes_gemini_38_flash_to_live_reasoning_wire_variants(self):
+        self.assertEqual(
+            resolve_wire_model_id("google-antigravity/gemini-3.8-flash", "minimal"),
+            "gemini-3.8-flash-low",
+        )
+        self.assertEqual(
+            resolve_wire_model_id("google-antigravity/gemini-3.8-flash", "off"),
+            "gemini-3.8-flash-low",
+        )
+        self.assertEqual(
+            resolve_wire_model_id("google-antigravity/gemini-3.8-flash", "medium"),
+            "gemini-3.8-flash-medium",
+        )
+        self.assertEqual(
+            resolve_wire_model_id("google-antigravity/gemini-3.8-flash", "high"),
+            "gemini-3.8-flash-high",
+        )
+
+    def test_gemini_38_flash_has_documented_output_cap_on_every_live_wire_variant(self):
+        self.assertEqual(KNOWN_MODELS["gemini-3.8-flash"], 65536)
+        for wire_id in (
+            "gemini-3.8-flash-low",
+            "gemini-3.8-flash-medium",
+            "gemini-3.8-flash-high",
+        ):
+            self.assertEqual(WIRE_PROFILES[wire_id]["maxOutputTokens"], 65536)
+
     def test_normalizes_live_wire_variants_to_logical_picker_ids(self):
         self.assertEqual(
             logical_model_id("gemini-3.7-flash-tiered"),
diff --git a/tests/test_picker_catalog.py b/tests/test_picker_catalog.py
new file mode 100644
index 0000000..23dd5bb
--- /dev/null
+++ b/tests/test_picker_catalog.py
@@ -0,0 +1,20 @@
+from unittest.mock import patch
+
+from antigravity_provider.hermes_provider import fetch_picker_model_ids
+
+
+def test_picker_catalog_uses_live_models_and_dedupes_wire_variants():
+    raw = {
+        "models": {
+            "gemini-3.7-flash-tiered": {},
+            "gemini-3.7-flash": {},
+            "gemini-2.5-pro": {},
+            "chat_internal": {},
+        }
+    }
+    with patch("antigravity_provider.runtime.fetch_available_models", return_value=raw):
+        assert fetch_picker_model_ids() == [
+            "gemini-3.7-flash",
+            "gemini-2.5-pro",
+            "gemini-3.8-flash",
+        ]
diff --git a/tests/test_transform.py b/tests/test_transform.py
index fe55ba4..7add485 100644
--- a/tests/test_transform.py
+++ b/tests/test_transform.py
@@ -56,6 +56,97 @@ class TransformTests(unittest.TestCase):
         self.assertEqual(body["model"], "openai/gpt-oss-120b-maas")
         self.assertEqual(body["request"]["generationConfig"]["thinkingConfig"]["thinkingBudget"], 8192)
 
+    def test_claude_tool_schema_flattens_terminal_notify_union(self):
+        body = build_generate_content_request(
+            model="claude-sonnet-4-6",
+            project_id="p",
+            messages=[{"role": "user", "content": "run a command"}],
+            tools=[{
+                "type": "function",
+                "function": {
+                    "name": "terminal",
+                    "description": "Run a command",
+                    "parameters": {
+                        "type": "object",
+                        "properties": {
+                            "command": {"type": "string"},
+                            "notify": {
+                                "description": "Notify on completion or matching lines",
+                                "anyOf": [
+                                    {"type": "boolean"},
+                                    {"type": "array", "items": {"type": "string"}},
+                                ],
+                            },
+                        },
+                        "required": ["command"],
+                    },
+                },
+            }],
+            reasoning_effort="off",
+        )
+        schema = body["request"]["tools"][0]["functionDeclarations"][0]["parameters"]
+        notify = schema["properties"]["notify"]
+        self.assertEqual(notify["type"], "boolean")
+        self.assertNotIn("anyOf", notify)
+
+    def test_gemini_tool_schema_keeps_terminal_notify_union(self):
+        body = build_generate_content_request(
+            model="gemini-3.7-flash",
+            project_id="p",
+            messages=[{"role": "user", "content": "run a command"}],
+            tools=[{
+                "type": "function",
+                "function": {
+                    "name": "terminal",
+                    "description": "Run a command",
+                    "parameters": {
+                        "type": "object",
+                        "properties": {
+                            "notify": {
+                                "anyOf": [
+                                    {"type": "boolean"},
+                                    {"type": "array", "items": {"type": "string"}},
+                                ],
+                            },
+                        },
+                    },
+                },
+            }],
+            reasoning_effort="off",
+        )
+        notify = body["request"]["tools"][0]["functionDeclarations"][0]["parameters"]["properties"]["notify"]
+        self.assertIn("anyOf", notify)
+
+    def test_claude_tool_history_preserves_tool_call_id(self):
+        body = build_generate_content_request(
+            model="claude-sonnet-4-6",
+            project_id="p",
+            messages=[
+                {"role": "user", "content": "Use the tool."},
+                {
+                    "role": "assistant",
+                    "tool_calls": [{
+                        "id": "call_history_123",
+                        "type": "function",
+                        "function": {"name": "get_time", "arguments": "{}"},
+                    }],
+                },
+                {"role": "tool", "tool_call_id": "call_history_123", "content": '{"time":"12:00"}'},
+            ],
+            tools=[{
+                "type": "function",
+                "function": {
+                    "name": "get_time",
+                    "parameters": {"type": "object", "properties": {}},
+                },
+            }],
+            reasoning_effort="off",
+        )
+        function_call = body["request"]["contents"][1]["parts"][0]["functionCall"]
+        function_response = body["request"]["contents"][2]["parts"][0]["functionResponse"]
+        self.assertEqual(function_call["id"], "call_history_123")
+        self.assertEqual(function_response["id"], "call_history_123")
+
 
 if __name__ == "__main__":
     unittest.main()
