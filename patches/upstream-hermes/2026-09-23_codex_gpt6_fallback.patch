diff --git a/hermes_cli/codex_models.py b/hermes_cli/codex_models.py
--- a/hermes_cli/codex_models.py
+++ b/hermes_cli/codex_models.py
@@ -13,6 +13,11 @@ import os
 logger = logging.getLogger(__name__)
 
 DEFAULT_CODEX_MODELS: List[str] = [
+    # GPT-6 family: live-verified through this account's Codex OAuth catalog.
+    # Keep these as offline fallbacks only; live OAuth discovery stays authoritative.
+    "gpt-6-astra",
+    "gpt-6-sol",
+    "gpt-6-luna",
     # GPT-5.6 series (Sol/Terra/Luna). The public API exposes "-pro"
     # variants, but the ChatGPT Codex OAuth backend rejects them with HTTP 400,
     # so the curated offline fallback must not surface those dead choices.
diff --git a/tests/hermes_cli/test_codex_models.py b/tests/hermes_cli/test_codex_models.py
--- a/tests/hermes_cli/test_codex_models.py
+++ b/tests/hermes_cli/test_codex_models.py
@@ -31,6 +31,18 @@ def test_curated_codex_fallback_excludes_chatgpt_rejected_pro_slugs(monkeypatch):
     assert CHATGPT_REJECTED_CODEX_PRO_SLUGS.isdisjoint(model_ids)
 
 
+def test_gpt6_offline_fallback_matches_live_catalog_without_synthetic_900k():
+    expected = {"gpt-6-astra", "gpt-6-sol", "gpt-6-luna"}
+
+    assert expected.issubset(DEFAULT_CODEX_MODELS)
+    offline = get_codex_model_ids(access_token=None)
+    assert expected.issubset(offline)
+    assert not any(
+        model.startswith("gpt-6-") and model.endswith("-900k")
+        for model in offline
+    )
+
+
 def test_picker_synthesizes_900k_variants_for_verified_slugs():
     """Every live-verified large-context slug gets an explicit ``-900k``
     picker variant directly after its base entry; slugs that genuinely
