"""Hermes Web Research Engine — fetcher package.

Architecture is FINAL. This package implements the approved Unified Executor
Interface, Browser Executor abstraction, Document contract, and adaptive
routing. Crawl4AI is the CURRENT browser implementation behind BrowserExecutor;
it is swappable without changing the Router.
"""

__version__ = "0.1.0"
