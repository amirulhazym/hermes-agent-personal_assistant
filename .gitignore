# Secrets
.env
*.key
*.pem
auth.json

# WhatsApp session (contains encryption keys and credentials)
session/
platforms/

# Hermes runtime
.hermes/

# Logs that may contain secrets or message content
*.log
logs/

# Health / PII data — NEVER commit (PDPA, public GitHub repo)
med-status.json
chain-state.json
med-schedule.json
med-supply.json
med-interactions.json
dexa_taper.json
substitutions.json
appointments.json
channel_directory.json
audit-prep/med-status.json
*.db
*.db-shm
*.db-wal

# OS files
.DS_Store
Thumbs.db

# Isolated implementation worktrees
.worktrees/
