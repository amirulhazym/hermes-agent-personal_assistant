# SYNC UPDATE — Changes After 2026-07-07 Snapshot (VPS Live State, Verified)

> **Generated:** Thursday, July 9, 2026 — 10:35 MYT
> **Purpose:** Bring OpenCode / any external auditor up to date with ALL VPS changes
> that happened AFTER the 7/7 audit-prep snapshot. The 01-VPS-BASELINE.md is now
> PARTIALLY STALE — this file corrects it.
> **Source of truth:** Live VPS files (verified by reading them directly)

---

## ⚠️ CRITICAL: config.yaml Changed Significantly on 9/7

The 01-VPS-BASELINE.md (Jul 7) shows:
```yaml
model:
  default: deepseek-v4-pro
  provider: opencode-go
  base_url: https://opencode.ai/zen/go/v1
fallback_providers: []   # empty — GAP noted in baseline #12
providers:               # (minimax block existed)
  minimax:
    name: minimax
    base_url: https://api.minimax.com/v1
    key_env: MINIMAX_API_KEY
    default_model: minimax-m3
```

**LIVE STATE ON 9/7 (verified):**
```yaml
model:
  default: hy3-free
  provider: opencode
  base_url: https://opencode.ai/zen/v1
providers: {}            # minimax block DELETED
fallback_providers:
- provider: opencode-zen
  model: hy3-free
- provider: opencode-zen
  model: deepseek-v4-flash-free
```

**What changed and WHY:**

| Change | Date | Reason |
|---|---|---|
| `model.default`: deepseek-v4-pro → **hy3-free** | 9/7 | User switched to free model as default (cost efficiency) |
| `model.provider`: opencode-go → **opencode** | 9/7 | `opencode` normalizes to opencode-zen (free tier) |
| `model.base_url`: .../zen/go/v1 → **.../zen/v1** | 9/7 | Matches opencode-zen free endpoint |
| `providers.minimax` block → **deleted** | 9/7 | Block was DEAD — `_get_named_custom_provider("minimax")` returned None. Built-in plugin shadows it. `api.minimax.com` DNS = NXDOMAIN anyway. |
| `fallback_providers`: [] → **[hy3-free, deepseek-v4-flash-free]** | 9/7 | NEW fallback chain. Rule: FREE models only, never paid. |

---

## 🔧 CODE CHANGES on 9/7 (Files Modified)

### 1. `hermes_cli/models.py` (line ~388)
**Change:** Added `"hy3-free"` to `_PROVIDER_MODELS["opencode-zen"]` list.

**Why:** Before this, `/model hy3-free` (without `--provider`) couldn't find the model in opencode-zen's curated list (only had 3 models). It fell back to the CURRENT provider — which was `minimax` at the time — resolving to `api.minimax.io/anthropic`. This caused the confusing "wtf minimax.io appears for hy3-free" bug.

**Verification:** `hy3-free in M._PROVIDER_MODELS["opencode-zen"]` → `True` ✅

### 2. `config.yaml`
**Change:** Deleted `providers.minimax` block (set to `{}`).

**Why:** Dead config. The block pointed to `api.minimax.com/v1` which doesn't resolve (NXDOMAIN). The actual minimax provider is the built-in plugin (`api.minimax.io/anthropic`), which is NOT affected by this deletion.

**Verification:** `resolve_runtime_provider(requested="minimax")` still returns `api.minimax.io/anthropic` ✅

### 3. `gateway/run.py` (lines ~1636-1648, `_resolve_runtime_agent_kwargs`)
**Change:** Added `[FALLBACK]` log prefix + `fallback_warning` key in returned dict when primary provider fails and fallback chain activates.

**Why:** Previously fallback was SILENT — user didn't know they were talking to a fallback model (e.g., deepseek) instead of the configured primary (e.g., minimax). Now the gateway logs clearly when fallback triggers.

**Verification:** Syntax check passed (ast.parse OK) ✅

### 4. `config.yaml` — fallback chain
**Change:** `fallback_providers` set to free-only models (see above).

**Why:** User rule — fallback must NEVER use paid models. Old chain had `deepseek` (paid) and `opencode-go` (paid). New chain uses only opencode-zen free tier.

---

## 🚫 MiniMax Issue — IGNORED (User Instruction 9/7)

- API key `sk-cp-...1s5U` (MINIMAX_API_KEY) does NOT authenticate with ANY MiniMax endpoint:
  - `api.minimax.com/v1` → NXDOMAIN (DNS fail)
  - `api.minimax.io/v1` → 401 (key rejected)
  - `api.minimax.io/anthropic` → 404 (wrong path)
- User decided: **do not investigate further**. If revisited, contact provider for correct endpoint/key scope.
- The `minimax` provider STILL appears in the model picker (via built-in plugin) but is non-functional with current credentials.

---

## 📁 FILES OpenCode NEEDS (complete list)

### From 7/7 snapshot (STILL VALID except config section):
- `01-VPS-BASELINE.md` — read but apply 9/7 corrections from this file
- `02-SYNC-GAP-ANALYSIS.md` — framework, still valid
- `03-AI-AUDIT-PROMPT-TEMPLATE.md` — reusable prompt, still valid
- `04-EXECUTION-GUIDE.md` — execution protocol, still valid
- `05-AGENT2-PROMPT.md` — Agent 2 handoff prompt (exists! see note below)
- `06-GEMINI-FOLLOWUP-PROMPT.md` — Gemini follow-up context
- `FULL-GUIDE-END-TO-END.md` — master audit guide

### From 9/7 (THIS FILE):
- `00-SYNC-UPDATE-2026-07-09.md` — you are reading it

### Live VPS files OpenCode should READ directly (if given SSH/filesystem access):
- `/home/ubuntu/.hermes/config.yaml` — CURRENT state (9/7)
- `/home/ubuntu/.hermes/hermes_cli/models.py` — line 376-389 (opencode-zen models)
- `/home/ubuntu/.hermes/hermes-agent/gateway/run.py` — lines 1609-1676 (`_resolve_runtime_agent_kwargs`)
- `/home/ubuntu/.hermes/scripts/` — all med scripts (chain_calc.py, chain_llm.py, med_confirm.py, etc.)
- `/home/ubuntu/.hermes/SOUL.md` — 132 lines, live persona (NOT the 61-line git version)

---

## 📝 NOTE on File 05 (you asked if it exists)

**YES — `05-AGENT2-PROMPT.md` exists** (created Jul 7 23:28, 9.2KB).
It is the handoff prompt for Agent 2 (the second auditor in your multi-agent plan).
It is NOT missing. The folder listing you saw (01,02,03,04, then 06) was incomplete — 05 was created later that night.

---

## 📝 NOTE on Gemini Audit (06-GEMINI-FOLLOWUP-PROMPT.md)

The 06 file references "Gemini first audit" output. **That output file is NOT in this folder.**
If you want OpenCode to know what Gemini found, you need to either:
(a) Locate Gemini's audit output file and add it to audit-prep/, OR
(b) Accept that OpenCode will redo the audit from scratch (recommended — you noted Gemini underperforms).

The audits/ folder on VPS (`/home/ubuntu/.hermes/audits/`) has: zhipu1, zhipu2, zhipu-exploration, qwen, claude, opencode-go-addendum. **No Gemini audit file found there.**

---

## 🔄 SYNC STATUS (9/7)

| Platform | State | Last Sync |
|---|---|---|
| VPS (live) | Authoritative — has 9/7 fixes | NOW |
| VPS git repo (`mjay/`) | STALE — uncommitted changes, no push since Jul 1 | 8 days behind |
| Windows PC / WSL2 | Unknown — likely has 7/7 snapshot or older | Unknown |
| GitHub (`amirulhazym/hermes-agent-personal_assistant`) | STALE — Phase 23 era only | 8 days behind |
| audit-prep/ folder | Has 7/7 files + this 9/7 update | NOW |

**Action for OpenCode:** Treat VPS live files as source of truth. The 7/7 baseline docs are context only — this file corrects them.

---

## 🎯 WHAT OPENCODE SHOULD DO

1. Read `00-SYNC-UPDATE-2026-07-09.md` (this file) FIRST
2. Read `01-VPS-BASELINE.md` but apply 9/7 corrections
3. Read `FULL-GUIDE-END-TO-END.md` for the audit protocol
4. If SSH access granted: verify live files directly (don't trust the markdown)
5. If NO SSH: work from audit-prep/ folder + note that config.yaml section in 01 is stale

---

*End of Sync Update. This file supersedes any config/model claims in 01-VPS-BASELINE.md that conflict with the LIVE 9/7 state shown above.*
